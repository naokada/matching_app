%p#notice= notice
%h1 Proposals
%table
  %thead
    %tr
      %th{:colspan => "3"}
  #proposal_list.flex
    - @proposals.each do |proposal|
      %div{id: "proposal#{proposal.id}"}
        = image_tag proposal.place.image_url, alt: proposal.place.name
        %h4 #{proposal.place.name}
        %p #{proposal.end_time.strftime("%H:%M")} まで滞在予定
%br/
.new_btn
  = link_to '席をシェアする', new_proposal_path
